import { TimeColorDirective } from './time-color.directive';

describe('TimeColorDirective', () => {
  it('should create an instance', () => {
    const directive = new TimeColorDirective();
    expect(directive).toBeTruthy();
  });
});
